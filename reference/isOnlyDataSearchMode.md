# Is Only Data SEARCH

Checks if "only-data-search mode" is active.

## Usage

``` r
isOnlyDataSearchMode()
```

## Value

(logical) TRUE if "only-data-search mode" is active, else FALSE for full
version of the app.
