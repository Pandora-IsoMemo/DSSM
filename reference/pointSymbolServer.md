# server function of leaflet point symbol settings module

server function of leaflet point symbol settings module

## Usage

``` r
pointSymbolServer(id, loadedData, apply)
```

## Arguments

- id:

  An ID string that corresponds with the ID used to call the module's UI
  function.

- loadedData:

  (reactive) loaded data

- apply:

  (reactive) apply button input
