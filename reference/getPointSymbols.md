# Get Point Size

Get Point Size

## Usage

``` r
getPointSymbols(df, columnForPointSymbol, symbols = unlist(pchChoices()))
```

## Arguments

- df:

  (data.frame) loaded data

- columnForPointSymbol:

  (character) name of the column that determines the point symbol

- symbols:

  (numeric) selected symbols
