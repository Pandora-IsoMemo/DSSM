# Filter Coord Square

Filter full grid of coordinates to square area

## Usage

``` r
filterCoordSquare(fullGrid, lat, long, length)
```

## Arguments

- fullGrid:

  grid of coordinates

- lat:

  (numeric) latitude of center

- long:

  (numeric) longitude of center

- length:

  (numeric) side length of square
