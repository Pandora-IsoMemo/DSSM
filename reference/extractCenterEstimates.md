# Extract Center Estimates

Extract Center Estimates

## Usage

``` r
extractCenterEstimates(XPredCenter, digits = 5, batch = FALSE)
```

## Arguments

- XPredCenter:

  (data.frame) data.frame containing estimates around a center

- digits:

  (numeric) significant digits

- batch:

  (logical) set TRUE if used for batch estimates
