# server funtion of leaflet point settings module

server funtion of leaflet point settings module

## Usage

``` r
pointSizeServer(id, loadedData, apply)
```

## Arguments

- id:

  An ID string that corresponds with the ID used to call the module's UI
  function.

- loadedData:

  (reactive) loaded data

- apply:

  (reactive) apply button input
