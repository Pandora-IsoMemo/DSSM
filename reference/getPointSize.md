# Get Point Size

Get point size in pixel

## Usage

``` r
getPointSize(df, columnForPointSize, sizeFactor = 1)
```

## Arguments

- df:

  (data.frame) loaded data

- columnForPointSize:

  (character) name of the column that determines the point size

- sizeFactor:

  (numeric) general factor for point size
