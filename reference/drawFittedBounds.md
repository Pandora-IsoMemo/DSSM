# Draw Fitted Bounds

Draw Fitted Bounds

## Usage

``` r
drawFittedBounds(map, showBounds, bounds)
```

## Arguments

- map:

  leaflet map

- showBounds:

  logical show/hide fitted bounds

- bounds:

  list of (west, east, south, north) boundaries to be drawn
