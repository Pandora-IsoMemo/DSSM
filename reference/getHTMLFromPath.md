# Get HTML from path

Creates the HTML string that defines the legend

## Usage

``` r
getHTMLFromPath(paths)
```

## Arguments

- paths:

  named list of paths to the files containing an icon, names of the
  items are used as labels
