# Location Fields Server

Backend for the module

## Usage

``` r
locationFieldsServer(id, dataRaw, dataSource)
```

## Arguments

- id:

  namespace id

- dataRaw:

  (reactive) raw data import

- dataSource:

  (reactive) source of loaded data, either "db" or "file" for database
  or file upload, respectively.
