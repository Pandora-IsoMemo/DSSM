# Get Default Coord Column

Get Default Coord Column

## Usage

``` r
getDefaultCoordColumn(columnNames, tryPattern = c("latitude", "^lat$"))
```

## Arguments

- columnNames:

  (character) column names of loaded data

- tryPattern:

  (character) pattern that should be matched with column names ordered
  after priority
