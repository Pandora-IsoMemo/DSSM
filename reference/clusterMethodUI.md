# Cluster Method UI

UI for the cluster method

## Usage

``` r
clusterMethodUI(ns, timeRangeInput = FALSE)
```

## Arguments

- ns:

  namespace

- timeRangeInput:

  logical, if TRUE, the time range input is shown
