# Customize Leaflet Map

Customize leaflet map for export

## Usage

``` r
customizeLeafletMap(leafletMap, leafletValues, zoom)
```

## Arguments

- leafletMap:

  leaflet map

- leafletValues:

  map settings, e.g. scalePosition, show/hide bounds

- zoom:

  zoom level
