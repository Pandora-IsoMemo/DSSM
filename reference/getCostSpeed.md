# Get Speed or Cost object for Cost-Surface and least-cost path

Get Speed or Cost object for Cost-Surface and least-cost path

## Usage

``` r
getCostSpeed(XPred, MinMax)
```

## Arguments

- XPred:

  (data.frame) data frame containing the spread function estimates

- MinMax:

  (string) are we estimating min or max

## Value

speed object
