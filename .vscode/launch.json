{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Active R Script",
            "type": "R-Debugger",
            "request": "launch",
            "env": {
                "R_PROFILE_USER": "${workspaceFolder}/.Rprofile"
            }
        }
    ]
}
